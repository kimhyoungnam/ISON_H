.answer.card
  .numbers.like-badge
    .number
      span.number.num-votes= question.numLikes
    .button
      a.like(href='#', data-id=answer.id)
        i.fa.fa-thumbs-up
        | 
        | join

  .card-body
    .content= answer.content
  .row.card-body
    .col-sm
    .col-sm.text-right
      .author
        | event on #[a(href=`/questions/_form/Date`)]
        if answer.author
          | 
          | by #[a(href=`/users/${answer.author.id}`)= answer.author.name]
